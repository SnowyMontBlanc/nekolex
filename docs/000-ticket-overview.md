# NekoLex チケット概要 & 依存関係図

## チケット一覧

| # | チケット名 | 概要 | 優先度 | 依存先 |
|---|-----------|------|--------|--------|
| 001 | プロジェクトクリーンアップ & 基盤整備 | テンプレ削除、ディレクトリ構成、テーマ定義 | 最高 | なし |
| 002 | 型定義 & 品種データベース作成 | TypeScript型、breeds.json (20品種) | 最高 | なし |
| 003 | タブナビゲーション構成変更 | 4タブ構成 (ホーム/図鑑/クイズ/プロフィール) | 最高 | 001 |
| 004 | ストレージサービス実装 | AsyncStorage ラッパー、データ永続化 | 高 | 002 |
| 005 | ユーザーContext & XPシステム | UserContext、レベル・XP計算 | 高 | 002, 004 |
| 006 | 画像処理サービス実装 | リサイズ、Base64変換、ファイル保存 | 高 | 001 |
| 007 | ~~Claude API連携サービス~~ → 018で置換 | ~~品種識別API~~ | ~~高~~ | ~~002, 006~~ |
| 008 | カメラ & 識別画面 | 撮影/ギャラリー選択、識別結果表示 | 高 | 003, 005, 007 |
| 009 | 図鑑一覧画面 | 品種カード、発見済み/未発見表示、進捗率 | 高 | 003, 005 |
| 010 | 品種詳細画面 | 動的ルート breed/[id]、特徴・性格表示 | 中 | 009 |
| 011 | クイズ生成ロジック | 難易度別出題、選択肢生成アルゴリズム | 中 | 002 |
| 012 | クイズ画面実装 | 難易度選択、4択UI、正解/不正解判定 | 中 | 003, 005, 011 |
| 013 | クイズ結果画面 | スコア表示、XP獲得、リトライ | 中 | 012 |
| 014 | プロフィール画面 | レベル・XP表示、統計情報、進捗 | 中 | 003, 005 |
| 015 | ホーム画面実装 | ダッシュボード、クイックアクション | 中 | 003, 005, 009 |
| 016 | アニメーション & 演出 | 発見時演出、正解/不正解、レベルアップ | 低 | 008, 012 |
| 017 | 復習機能強化 (間隔反復) | スペースド・リピティション、復習スケジュール | 低 | 012 |
| 018 | Gemini API移行 | Claude API → Gemini 2.5 Flash 無料枠に移行 | 高 | 007 |
| 019 | ダークモード修正 — ホーム画面 | index.tsx のハードコードカラー修正・ネオングロウ適用 | 高 | 018 |
| 020 | ダークモード修正 — 図鑑系 | pokedex.tsx + breed-card.tsx のハードコードカラー修正 | 高 | 018 |
| 021 | ダークモード修正 — クイズ系 | quiz.tsx + quiz-question.tsx + quiz-result.tsx のカラー修正 | 高 | 018 |
| 022 | ダークモード修正 — プロフィール | profile.tsx のハードコードカラー修正 | 高 | 018 |
| 023 | テーマカラー刷新 | theme.ts カラーパレット全体の見直し・刷新 | 中 | 018 |
| 024 | タイポグラフィシステム定義 | Typography トークンを theme.ts に追加、最小 12px ルール制定 | 中 | 023 |
| 025 | タイポグラフィ全ファイル適用 | 全画面・コンポーネントの fontSize をトークンに置換、9/10/11px 廃止 | 中 | 024 |
| 026 | 触覚フィードバック正式実装 | haptic_enabled 設定を連動、クイズ/発見/レベルアップに振動追加 | 中 | なし |
| 027 | サウンドフィードバック実装 | expo-av で効果音実装、sound_enabled 設定と連動 | 低 | 026 |
| 028 | レベルアップ演出実装 | レベルアップ検知 + LevelUpAnimation オーバーレイ表示 | 中 | 005, 027 |

## 依存関係図

```
Phase 6 (019〜022 は並行可能、023 も並行可能):

        ┌──────────────────────────────────────────────────────────────┐
        │                        018 完了後                            │
        ├──────────┬──────────┬──────────┬──────────┬─────────────────┤
        ▼          ▼          ▼          ▼          ▼                 │
  ┌──────────┐┌──────────┐┌──────────┐┌──────────┐┌──────────┐       │
  │ 019      ││ 020      ││ 021      ││ 022      ││ 023      │       │
  │ ホーム   ││ 図鑑系   ││ クイズ系 ││ プロフ  ││ テーマ   │       │
  │ ダーク   ││ ダーク   ││ ダーク   ││ ダーク  ││ カラー   │       │
  │ 修正     ││ 修正     ││ 修正     ││ 修正    ││ 刷新     │       │
  └──────────┘└──────────┘└──────────┘└──────────┘└──────────┘       │
        全て独立・並行可能 (変更ファイルが重複しない)                  │
```

全体依存関係図:
```
                    ┌───────────┐     ┌───────────┐
                    │ 001       │     │ 002       │
                    │ クリーン  │     │ 型定義 &  │
                    │ アップ    │     │ 品種DB    │
                    └─────┬─────┘     └──┬──┬──┬──┘
                          │              │  │  │
              ┌───────────┤      ┌───────┘  │  └────────────┐
              │           │      │          │               │
              ▼           ▼      ▼          ▼               ▼
        ┌───────────┐ ┌───────────┐  ┌───────────┐  ┌───────────┐
        │ 006       │ │ 003       │  │ 004       │  │ 011       │
        │ 画像処理  │ │ タブ構成  │  │ ストレージ│  │ クイズ    │
        │ サービス  │ │ 変更      │  │ サービス  │  │ ロジック  │
        └─────┬─────┘ └──┬──┬──┬──┘  └─────┬─────┘  └─────┬─────┘
              │          │  │  │            │               │
              ▼          │  │  │            ▼               │
        ┌───────────┐    │  │  │     ┌───────────┐          │
        │ 007       │    │  │  │     │ 005       │          │
        │ Claude    │    │  │  │     │ ユーザー  │          │
        │ API連携   │    │  │  │     │ Context   │          │
        └─────┬─────┘    │  │  │     └──┬──┬──┬──┘          │
              │          │  │  │        │  │  │              │
              │    ┌─────┘  │  └────┐   │  │  │              │
              │    │        │       │   │  │  │              │
              ▼    ▼        │       ▼   ▼  │  │              │
        ┌───────────┐       │ ┌───────────┐│  │              │
        │ 008       │       │ │ 009       ││  │              │
        │ カメラ &  │       │ │ 図鑑一覧  ││  │              │
        │ 識別画面  │       │ └─────┬─────┘│  │              │
        └─────┬─────┘       │       │      │  │              │
              │             │       ▼      │  │              │
              │             │ ┌───────────┐│  │              │
              │             │ │ 010       ││  │              │
              │             │ │ 品種詳細  ││  │              │
              │             │ └───────────┘│  │              │
              │             │              │  │              │
              │             │    ┌─────────┘  │              │
              │             │    │            │              │
              │             ▼    ▼            │              │
              │       ┌───────────┐           │              │
              │       │ 012       │◄──────────┼──────────────┘
              │       │ クイズ    │           │
              │       │ 画面     │           │
              │       └─────┬─────┘           │
              │             │                 │
              │             ▼                 │
              │       ┌───────────┐           │
              │       │ 013       │           │
              │       │ クイズ    │           │
              │       │ 結果画面  │           │
              │       └───────────┘           │
              │                               │
              │       ┌───────────┐           │
              │       │ 014       │◄──────────┘
              │       │ プロフ    │
              │       │ ィール    │
              │       └───────────┘
              │
              │       ┌───────────┐
              ▼       │ 015       │◄── 003, 005, 009
        ┌───────────┐ │ ホーム    │
        │ 016       │ │ 画面     │
        │ アニメ    │ └───────────┘
        │ ーション  │
        └───────────┘
              │
              ▼
        ┌───────────┐
        │ 017       │
        │ 復習機能  │
        │ 強化      │
        └───────────┘
```

## 並行開発ガイド

### Phase 1: 基盤 (並行可能)
- **Agent A**: `001` プロジェクトクリーンアップ & 基盤整備
- **Agent B**: `002` 型定義 & 品種データベース作成

### Phase 2: サービス層 (001, 002 完了後、並行可能)
- **Agent A**: `003` タブナビゲーション構成変更
- **Agent B**: `004` ストレージサービス + `006` 画像処理サービス
- **Agent C**: `011` クイズ生成ロジック

### Phase 3: コア機能 (Phase 2 完了後、並行可能)
- **Agent A**: `005` ユーザーContext & XPシステム → `018` Gemini API連携
- **Agent B**: `009` 図鑑一覧画面 → `010` 品種詳細画面
- **Agent C**: `014` プロフィール画面

### Phase 4: メイン画面 (Phase 3 完了後、並行可能)
- **Agent A**: `008` カメラ & 識別画面
- **Agent B**: `012` クイズ画面 → `013` クイズ結果画面
- **Agent C**: `015` ホーム画面

### Phase 5: 仕上げ
- **Agent A**: `016` アニメーション & 演出
- **Agent B**: `017` 復習機能強化

### Phase 6: UI刷新 (全て並行可能)
- **Agent A**: `019` ダークモード修正 — ホーム画面
- **Agent B**: `020` ダークモード修正 — 図鑑系
- **Agent C**: `021` ダークモード修正 — クイズ系
- **Agent D**: `022` ダークモード修正 — プロフィール
- **Agent E**: `023` テーマカラー刷新

### Phase 7: タイポグラフィ整備
- **Agent A**: `024` タイポグラフィシステム定義 (完了)
- **Agent B**: `025` タイポグラフィ全ファイル適用

### Phase 8: フィードバック実装 (並行可能)
- **Agent A**: `026` 触覚フィードバック正式実装
- **Agent B**: `027` サウンドフィードバック実装 (`026` 完了後)

### Phase 9: レベルアップ演出
- **Agent A**: `028` レベルアップ演出実装 (`027` 完了後)
